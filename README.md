# exshin.github.io
Personal Website

To run: `npm run dev`
To build: `npm run build`
To deploy: `npm run deploy`

If asked for a username and password, give the github username and the password is the Github Personal Access Token